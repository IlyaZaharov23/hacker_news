import { black, semiLightGray } from "constants/colors";
export const styles = {
  button: {
    fontSize: "0.75rem",
    fontWeight: "600",
    color: black,
    "&:hover": {
      backgroundColor: semiLightGray,
    },
  },
};
